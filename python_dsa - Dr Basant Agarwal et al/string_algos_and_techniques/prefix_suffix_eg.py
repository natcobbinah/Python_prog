string = "this is data structures book by packt publisher"
suffix = "publisher"
prefix = "this"

print(string.endswith(suffix)) # check if the string contains given suffix
print(string.startswith(prefix)) # check if string starts with given prefix