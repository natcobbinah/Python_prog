greet = "hello word"
# identity = greet
# type = <class of object, and its internal representation>
# value = 'hello world'
# identity of object can be obtained using id()
print(type(greet))
print(id(greet)) # integer representation of pointer greet in memory