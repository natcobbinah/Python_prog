# immutable list = tuples in python
dimensions = (200,50)
for dimension in dimensions:
    print(dimension)

# throws an error before values in tuples are immutable
# dimensions[0] = 2390

# technically tuples are defined by the presence of a comma
# the parentheses make them look neater and readable
my_tuple = (3,)
print(my_tuple)
